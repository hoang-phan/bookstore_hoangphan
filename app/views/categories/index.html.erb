<% provide(:title, "All categories") %>
<div id="content1">
  <div class="jumbotron">
    <% @categories.each do |category| %>
    <li>
    <%= link_to category.name ,category_path(category) %>
    </li>
    <% end %>
  </div>
</div>